{
  "name": "packaging-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.18",
    "concurrently": "^9.0.0",
    "tailwindcss": "^4.1.18"
  }
}
